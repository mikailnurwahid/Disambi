import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import Disambi from './routes'

export default function App() {
  return (
    <Disambi/>
  )
}

const styles = StyleSheet.create({})